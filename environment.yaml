name: lrann 
channels:
  - conda-forge
  - defaults
  - pytorch
dependencies:
  - python=3.6
  - jupyterlab
  - pandas
  - pytorch>=1.0
  - scipy
  - tqdm
  - requests
  # needed for running the unit tests
  - pytest
  - pytest-cov

